using Microsoft.Owin.Hosting;
using System;
using System.ServiceModel;

namespace IBAR.SyncerManager
{
    class Program
    {
        static void Main(string[] args)
        {
            //ServiceHost host = new ServiceHost(typeof(Service));

            //host.Open();
            //Console.WriteLine("Server");
            //Console.ReadKey();
            //host.Close();

            //using (WebApp.Start("http://localhost:8001"))
            //{
            //    ServiceHost host = new ServiceHost(typeof(Service));
            //    host.Open();
            //    Console.WriteLine("Server");
            //    Console.ReadKey();
            //    host.Close();
            //}

            //Uri httpUrl = new Uri("net.tcp://localhost:8000/IContract");
            //ServiceHost host = new ServiceHost(typeof(Service), httpUrl);
            //    host.Open();
            //Console.WriteLine("Service is Hosted at {0}", DateTime.Now.ToString());
            //Console.WriteLine("Host is running...Press  key to stop the service.");
            //Console.ReadLine();
            //    host.Close();
            
        }
    }
}
